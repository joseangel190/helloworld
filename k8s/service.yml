apiVersion: v1
kind: Service
metadata:
  name: helloworld-service
  namespace: helloworld
spec:
  selector:
    app.kubernetes.io/name: helloworld
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
